pipeline {
    agent any
    stages {
        stage('Clone Repository') {
            steps {
                // Update this to use the 'main' branch
                git branch: 'main', url: 'https://github.com/SaeidNK/srerepo.git'
            }
        }
        // Other pipeline stages...
    }
}
